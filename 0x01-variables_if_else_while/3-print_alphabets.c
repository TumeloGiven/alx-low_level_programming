#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Return: 0
 */

int main(void)
{
	char lowc;

	for (lowc = 'a'; lowc <= 'z'; lowc++)
	putchar(lowc);

	for (lowc = 'A'; lowc <= 'Z'; lowc++)
	putchar(lowc)
	putchar(lowc);
	return (0);
}
